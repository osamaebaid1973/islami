import 'package:flutter/material.dart';

class SuraNameItem extends StatelessWidget {
  const SuraNameItem({super.key});
    int x;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}